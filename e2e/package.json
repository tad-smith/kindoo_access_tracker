{
  "name": "@kindoo/e2e",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "End-to-end Playwright suite for the Kindoo SPA. Phase 1 ships one smoke spec; flow-based suites land alongside Phases 4-10 of the Firebase migration.",
  "scripts": {
    "test": "playwright test",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "lint": "prettier --check tests fixtures",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^22.10.5"
  }
}
