{
  "name": "kindoo-access-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "Google Apps Script web app for managing Kindoo door-access seat assignments across a stake of The Church of Jesus Christ of Latter-day Saints.",
  "scripts": {
    "login": "clasp login",
    "logout": "clasp logout",
    "version:stamp": "node scripts/stamp-version.js",
    "push": "npm run version:stamp && clasp push",
    "push:watch": "npm run version:stamp && clasp push --watch",
    "pull": "clasp pull",
    "open": "clasp open",
    "open:web": "clasp open --webapp",
    "deploy": "clasp deploy",
    "deployments": "clasp deployments",
    "logs": "clasp logs",
    "status": "clasp status"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  },
  "engines": {
    "node": ">=18"
  }
}
