# Chunk N — <title>

**Shipped:** YYYY-MM-DD
**Commits:** <commit range, tag, or PR link>

## What shipped

One or two sentences on the user-visible outcome of this chunk.

## Deviations from the pre-chunk spec

Things this chunk does that the pre-chunk `spec.md` didn't say, with reasoning. The spec has been updated in this chunk's commits to match. Leave the section empty (or delete it) if there were none.

- **<deviation>** — <why>. Spec: <which section changed>.

## Decisions made during the chunk

Decisions that came up during build and are load-bearing but didn't rise to a spec change. Usually new architecture.md D-numbers or open-questions.md resolutions.

- <decision> — <why>. Recorded as: <D-number / Q-number>.

## Spec / doc edits in this chunk

One-liner per touched file.

- `docs/spec.md` — <change>
- `docs/architecture.md` — <change>
- `docs/data-model.md` — <change>
- `docs/open-questions.md` — <change>
- `docs/build-plan.md` — <change>

## Deferred

Things intentionally left out of this chunk, with where they land.

- <item> → Chunk M

## Next

Anything the next chunk needs to know that isn't already in `build-plan.md`.
